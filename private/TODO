To do
=====

- earlier param type checking (avoid checking within lock!)

- add development section to docs

- test odbc

----

- add "[fatal]" to fatal error messages (where feasible)

- some parts of docs are Linux-centric, especially ODBC part; broaden

- clean up le/be/host in generic/io, odbc

- export and doc kill-safe wrapper

- permit type annotations for
  - query params (would help mysql, odbc)
  - extra conversions on result fields (eg sqlite, specify date field)

- sqlite: opportunistic conversions based on decltype (eg date, time)

- add fsym arguments to type-writers

- disconnect should always work, even on damaged connections
  (eg kill-thread, custodian shutdown)

- add auto-prepared-statement:
  - has weak hash of connection => prepared statement,
    on (query-* auto-pst blah ...), prepares stmt if not already in table
  - allow dbsystem-dispatch (would be great for testing...)

----

bug: mysql concurrency tests sometimes fail
  - temporary fix: always wrap mysql connection in kill-safe proxy

bug: mysql blob parsing differs (text vs binary)

----

add extra sanity-checking mode (connection option?)
  - check server_encoding, print message if SQL_ASCII
    eg: (query-value c "show server_encoding") => "UTF8"
  - check postgresql typeids as expected?
